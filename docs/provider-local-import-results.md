# Local Phase 2 import results

Executed on 2026-09-21 in the existing local Supabase project `Lia-vaud`.

Target: `postgresql://127.0.0.1:54322/postgres`, accessed through `psql` inside `supabase_db_Lia-vaud` over its Unix socket. The pinned local Docker Desktop Linux engine, project/workspace labels, database port mapping, PostgreSQL 17 image and SQL identity checks passed. Docker Desktop was started; its existing Supabase containers resumed. No Supabase reset, migration or remote connection was performed.

## Observed sequence

| Operation | Result |
| --- | --- |
| Initial database inspection | 0 providers, 0 sources, 0 service areas; public.leads absent |
| `node --test tests/*.test.mjs` | 18 passed, 0 failed, 0 skipped |
| Targeted ESLint on the new JavaScript modules/tests | Passed |
| `node scripts/ingest-legacy-providers.mjs --check-report` | Passed; 66 verification-required, 42 special review |
| `node scripts/import-local-providers.mjs --dry-run` | 66 valid, 0 rejected, 0 duplicate IDs/slugs, 0 writes |
| `node scripts/test-local-provider-writer.mjs --write-local --confirm Lia-vaud:local:54322` | Passed; all fixtures rolled back |
| `node scripts/import-local-providers.mjs --write-local --confirm Lia-vaud:local:54322` | Committed 66 providers + 66 sources; 0 skipped |
| `node scripts/import-local-providers.mjs --verify-local` | Passed; all 66 exact provider/provenance matches |
| Identical local write command, second execution | 0 inserted, 66 skipped; still 66 providers + 66 sources |

The PostgreSQL integration tests proved first insertion and an identical rerun with complete rows (including UUIDs and timestamps) unchanged. Name, legacy ID, slug, publication/verification/review state, lifecycle, source-note and duplicate-source conflicts were rejected without overwriting. An error after all inserts and a late identity collision both rolled back the entire batch. The final test transaction also rolled back, leaving the local tables empty before the actual import.

## Verified committed state

| Check | Result |
| --- | --- |
| Providers / private sources | 66 / 66 |
| EMS / domicile / residence | 46 / 12 / 8 |
| Unique legacy IDs / slugs | 66 / 66 |
| Exactly matching provider and single legacy provenance source | 66 |
| Published / verified | 0 / 0 |
| Unverified / unpublished | 66 / 66 |
| Directory lifecycle active | 66; no assertion of real-world operation |
| Null canton / municipality / last review | 66 / 66 / 66 |
| Service areas / service codes / language codes | 0 / 0 / 0 |
| Visible providers under anon / authenticated | 0 / 0 |
| Public source SELECT privilege for anon / authenticated | None / none |
| Municipality rows | Unchanged |
| public.leads | Absent before and after; not created or altered |

Every transformed provider/source field was checked, including exact original tags/location text, empty subtypes/attributes, null structured street/coordinates/contacts/description, source notes, supported fields, and null source URL/retrieval/review timestamps. UUIDs and creation/update timestamps were generated by PostgreSQL.

The leads check in this environment proves continued absence, not preservation of a populated leads table. No production leads or other production data were inspected. When a local leads table exists, the implementation checks its count and full-row digest inside the transaction while holding a read lock.

All 66 still require verification before publication: 42 have additional structured concerns; 24 require baseline verification only. Records remain separate. Mechanical import success is not publication approval.

No new migration was required. Source idempotency uses transaction-level table locks and exact deterministic provenance matching. Public UI, public.leads, schema/RLS/grants and the pure transform were not changed. No remote Supabase access, commit or push occurred.

## Batch-scoping follow-up

The local writer now requires exactly one source matching the expected legacy identity (provider UUID, source type, source name, external record ID), not exactly one source total. That source must still match every expected provenance field; other sources remain untouched. Verification counts, state/coverage checks and anon/authenticated visibility are scoped to the expected legacy IDs. JSON output explicitly reports `scope: legacy-batch`.

Follow-up checks passed:

- All 18 unit tests and targeted ESLint passed. Dry-run remained 66 valid, 0 rejected, 0 database writes.
- The rollback-only PostgreSQL suite recreated the existing Phase 1 migration in an isolated test schema without clearing or modifying imported public tables. First import inserted 66 providers/sources; the second inserted 0 and skipped 66. All previous conflict and rollback assertions passed.
- With 67 fixture providers, 70 sources and one unrelated service area, verification still found exactly 66 batch providers and 66 matching legacy sources. The unrelated provider was published/verified, had canton GE and services/languages/coverage, and was outside the legacy batch. The three extra sources on a legacy provider each differed in one source identity component; a fourth source on the unrelated provider shared the other legacy source identity components. Rerunning skipped all 66 and preserved all fixture provider/source/coverage rows exactly.
- Duplicate matching legacy evidence, missing matching legacy evidence, and mismatched legacy provenance all failed. The test namespace and all fixtures rolled back.
- Actual local `--verify-local` passed: 66 providers, 66 matching legacy sources, 46 EMS / 12 domicile / 8 residences, 66 unverified/unpublished/null-canton records, zero services/languages/coverage, 66 unique legacy IDs/slugs, and zero legacy providers visible under anon/authenticated.
- Actual local importer rerun: **0 inserted, 66 skipped**. Municipalities remained unchanged; public.leads remained absent. No existing source was overwritten or deleted.

No migration, remote access, commit or push was performed in this follow-up.
